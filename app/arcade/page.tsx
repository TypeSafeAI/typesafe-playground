import { SectionPage } from "../../components/SectionPage";
import { pageMetadata } from "../../lib/social";
export const metadata = pageMetadata("arcade");
export default function Page() {
  return <SectionPage id="arcade" />;
}
