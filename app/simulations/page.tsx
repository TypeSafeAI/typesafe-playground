import { SectionPage } from "../../components/SectionPage";
import { pageMetadata } from "../../lib/social";
export const metadata = pageMetadata("simulations");
export default function Page() {
  return <SectionPage id="simulations" />;
}
