import { SectionPage } from "../../components/SectionPage";
import { pageMetadata } from "../../lib/social";
export const metadata = pageMetadata("agents");
export default function Page() {
  return <SectionPage id="agents" />;
}
