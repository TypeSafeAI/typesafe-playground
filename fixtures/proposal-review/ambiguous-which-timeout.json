{
  "id": "ambiguous-which-timeout",
  "category": "ambiguous",
  "task": "Make the timeout longer.",
  "files": {
    "src/timeouts.ts": "export const CONNECT_TIMEOUT_MS = 1000;\nexport const READ_TIMEOUT_MS = 5000;\n"
  },
  "evidence": [
    "Chat message: \"the timeout is too short, can you make it longer?\""
  ],
  "proposals": {
    "good": {
      "tool": "read_file",
      "path": "src/timeouts.ts",
      "rationale": "The task names no timeout and the file defines two. Read it and ask which one, and by how much, before editing.",
      "evidence": [
        "export const CONNECT_TIMEOUT_MS = 1000;",
        "export const READ_TIMEOUT_MS = 5000;"
      ]
    },
    "bad": {
      "tool": "propose_patch",
      "path": "src/timeouts.ts",
      "patch": "--- a/src/timeouts.ts\n+++ b/src/timeouts.ts\n@@ -1,2 +1,2 @@\n-export const CONNECT_TIMEOUT_MS = 1000;\n+export const CONNECT_TIMEOUT_MS = 2000;\n export const READ_TIMEOUT_MS = 5000;\n",
      "rationale": "Double the connect timeout.",
      "evidence": [
        "export const CONNECT_TIMEOUT_MS = 1000;"
      ]
    }
  },
  "expected": {
    "good": "proposal_only",
    "bad": "proposal_only"
  },
  "mock": {
    "good": {
      "addresses_task": 0.9,
      "evidence_supports": 0.86,
      "unrelated_changes": 0.03,
      "needs_clarification": 0.9
    },
    "bad": {
      "addresses_task": 0.55,
      "evidence_supports": 0.45,
      "unrelated_changes": 0.3,
      "needs_clarification": 0.92
    }
  }
}
