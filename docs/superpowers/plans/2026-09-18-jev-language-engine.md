# Jev language engine implementation plan and execution ledger

> Execute in this existing user-authorized dirty worktree. Preserve unrelated work. Use test-driven implementation and subagent-driven-development for the isolated cryptographic module; integrate and review in the main agent. No commits without explicit delivery scope and verification.

**Goal:** Implement the previously agreed Jev-only semantic planning, bounded creative composition, cryptographic response graph, independent engine, polished chat integration, and evaluation workflow. Retain the user's full objective of matching or exceeding leading LLM quality as an unproven completion requirement; functional tests cannot close that requirement.

**Architecture:** Pure TypeScript engine with injected Jev transport. Local source segmentation, authored proposition catalog and grammar build candidate semantic plans. Jev batches independent interpretation/evidence questions, code builds compatible realizations, then a bounded second request evaluates new candidate responses. Exact sources, hypothetical constructions, missing evidence, and failures remain distinct. Web Crypto SHA-256 authenticates internal graph integrity, never truth or author identity. The existing selector remains a baseline.

**Stack:** Existing Node/pnpm/TypeScript/Next/React, Web Crypto, node:test, Playwright. No dependency or provider additions.

## Requirements and proof

- [x] `lib/jev-chat/graph.ts`: canonical node hashing, bounded DAG, deterministic rendering and verification, duplicate sharing, corruption rejection. `tests/jev-chat-graph.test.ts` independently mutates leaf text, child order, graph references, root, and cycles.
- [x] `lib/jev-chat/types.ts`, `knowledge.ts`, `grammar.ts`: initial typed propositions, source-backed passages, reusable semantic operations (capabilities, explanation, comparison, summary, clarification, support, fiction); qualifications survive composition; no model-authored code execution. These bounded operations do not establish general semantic competence.
- [x] `lib/jev-chat/engine.ts`: injectable transport, structured conversational context, bounded batched requests, strict complete response parsing, selected probability and concentration kept distinct, whole-plan assessment, stop/deadline guards, no live-to-demo fallback.
- [x] `tests/jev-chat-engine.test.ts` and evaluation tests: capabilities in every space, contextual follow-ups, unknowns, a synthetic source-instruction trap, exact source evidence, composition, fictional provenance, rejected candidate fallback, corruption, malformed provider output, cancellation, bounds, deterministic replay. New regression failures observed before fixes; no general prompt-injection or semantic-quality claim.
- [x] `lib/jevChat.ts` and `lib/jev-chat/storage.ts`: preserve baseline and migrate saved chats without silent loss; restore composed messages through allowlisted validation and graph verification; consistent bounded saves and explicit recovery preserve original bytes; keep credentials excluded.
- [x] `components/JevChatLab.tsx`, focused engine UI components and scoped CSS: engine selector, expressive topics, accurate progress, source/provenance chips, inspectable graph, candidate traces, recovery, mobile/keyboard/themes. Preserve existing draft/key/cancellation behavior.
- [x] `scripts/jev-chat-eval.ts`, separate `scripts/jev-chat-benchmark.ts`, evaluation fixtures/report schema: baseline/composition side-by-side, response artifacts, independent exact checks, explicit unmeasured semantic quality, opt-in live mode bounded without automated shared-credit use; imported reviews remain separate from model judgments and their attribution is unverified.
- [x] `docs/jev-chat.md`, README/nav metadata: describe actual capabilities, operations, privacy, source bounds, budgets, model limitations and evaluation gaps.
- [x] Verify targeted tests, typecheck, build, focused production browser scenarios and legacy tests. Inspect screenshots. Broader suites only as needed; existing Clean Room tests contend for two job slots under parallel execution.
- [ ] Benchmark live Jev on reviewed held-out semantic/creative/multi-turn cases and compare with current leading-model outputs under matched conditions. No success claim without this evidence. Provider access, authorized comparison responses, independently reviewed results and benchmark coverage remain to establish.

## Initial execution state

2026-09-18: existing chat is a whole-reply selector with three topics, fixed 0.8 gate, local demo, persistent threads, and mocked e2e coverage. User supplied live screenshot demonstrates unsupported capabilities handling. No live semantic superiority evidence exists. Worktree also contains unrelated YouTube-extraction and local-browser changes; those are protected.

## Current execution evidence

- Shared engine implemented inside the playground with injected transport and no React/Next/network runtime dependency. The offline CLI is a second host. Keep a future standalone app on this same engine; extract a versioned package when a second application needs it.
- Offline fixture run: composition 28/28 mechanical cases, baseline 15/28; zero provider calls and 28 verified composition graphs. These fixtures guided subsequent fixes and are not held-out evaluation. Semantic/creative quality, live latency/cost and leading-model comparison remain unmeasured.
- Fresh verification: `pnpm test` passed 36 JavaScript and 269 TypeScript tests; `pnpm typecheck` and `pnpm build` passed; Python suite passed 26 tests. Independent review confirmed fixes for storage loss, shipped demo prompts and actual transport deadline cancellation.
- Production browser run: 14/14 tests pass, including storage recovery/export/cancelled replacement, baseline, mocked live decisions/errors, keyboard, saved drafts, both themes and 320/390/1280 widths. Screenshots inspected. Short mobile retains at least160px of transcript; overflowing usage controls fixed. Visibility checks account for measured subpixel scroll rounding with at most one physical pixel of edge tolerance. No live API, deployment, commit or package publication performed.
- Still required: expand useful semantic operations and creative control beyond the bounded initial grammar; establish independently reviewed held-out live Jev and matched leading-model results. Offline evaluation and opt-in live benchmark tooling are implemented; no live comparison evidence exists. The full user objective remains active and unproven.

## Next capability increment: source reasoning and live measurement

The previous goal turn is classified as **progress**: engine/UI/storage changes and verification changed authoritative state. The next increment addresses a real capability gap: source comparison currently quotes passages without calculating relationships.

- [x] `lib/jev-chat/quantities.ts` and `tests/jev-chat-quantities.test.ts`: bounded exact source spans, units/context, conservative complete numeric boundaries, exact rational arithmetic and source-bound derivation hashes. Independently reviewed after fixes for numeric tails, unknown predicates, dates, accessors and key budgets. 25 quantity tests pass. No arbitrary code execution, currency conversion or general entailment claim.
- [x] `reasoning.ts` and engine integration: operation/two operand choices in the first Jev batch; complete response assessment in the second. Unknown, incompatible, low-confidence or conflicting selections clarify. Local demo rejects unsupported multiplicity, compound operations, partial references and literal duplicate facts; ratios require live operand selection. Demo grammar remains explicitly limited.
- [x] Calculation details show source figures, exact equation and a separate fingerprint. Saved calculations replay full ordered notes and bind the exact source set and rendered response. Stripped proofs, duplicate source substitutions and tampering fail; ordinary quotations resembling calculation labels still restore.
- [x] `benchmark.ts`, explicit CLI and 21 mocked tests: bounded requests, new output requirement, atomic checkpoints, unknown/interrupted usage, cancellation and rate-limit termination; no late writes from ignored aborts. Imports bind exact comparison text, separate human/model judgments and replay completed engine decisions against context/request hashes. Current engine version required for replay; identities and actual provider execution remain unauthenticated.
- [x] Fresh verification: `pnpm test` 36 JavaScript +323 TypeScript tests; `pnpm typecheck`; explicit TypeScript checking for CLI/benchmark/calculation tests with `--ignoreConfig`; `pnpm build`; 16/16 focused production browser tests. Inspected light/dark calculation screenshots at320px. `git diff --check` passes. Python coverage was not rerun for this TypeScript-only increment; prior phase passed26tests.

Eight new synthetic development fixtures in `tests/fixtures/jev-chat-reasoning.json` pass8/8 composition mechanical cases versus4/8 baseline cases, with zero provider calls. These fixtures guided development and are not held-out quality evidence. An offline plan for two of these cases with a four-request limit was generated successfully; it was not executed live. No commits, publishing, deployments, real provider calls or comparative quality claims were made.

Remaining work is still substantive: evaluate actual live Jev semantic selection on separately reviewed cases, collect matched comparison outputs and independent reviews, measure latency/cost, and expand useful language/creative operations based on observed failures. The full objective cannot be closed by the implementation and mechanical tests above.

The current TypeSafe documentation confirms mixed typed questions can be sent together and evaluated independently against shared state; dependent steps must be composed by application code. Reference: https://docs.typesafe.ai/introduction . No inference about semantic quality follows from that API contract.

## Next increment: evidence planning and live request validity

The previous goal turn was **progress**: arithmetic, persisted proofs, benchmark replay and browser verification changed authoritative state. Current inspection found a live API contract bug and an evidence-planning gap: zero numeric facts produce invalid one-option choices; ranked demo relevance is lost when grammar restores original source order; source plans silently cap non-summary answers at two, three or five passages.

- [x] Reproduce and fix empty operand choices, validate every outgoing live request with the existing API schema, and test nonnumeric/qualified/single-quantity notes through that validator. No extra provider calls.
- [x] Preserve ranked evidence judgments through composition, with stable source-order ties and original source order in comprehensive extracts. Keep the 0.8 selection gate and exact source wording.
- [x] Offer a complete extract candidate for larger relevant source sets within existing source/graph bounds, disclose omitted selected passages in every partial source plan, and ask the second-stage judge to check all parts of the user's task. Complete evidence inclusion is not semantic completeness or truth.
- [x] Bound candidate alternatives within the existing request budget by removing redundant alternatives, not truncating source or conversation text. Keep the complete extract when one is available and assess every live factual answer. Verified a JSON-escaping boundary that requires keeping only the full extract; no source/history loss, still two requests.
- [x] Added ranking, coverage, request-budget, persistence and mocked live-note browser regressions. Full suite passed36 JavaScript+329 TypeScript tests; an additional budget boundary brought the focused evidence suite to6/6. Typecheck, explicit ES2022 test/CLI checking and production build passed. Replaced Array.findLastIndex with an equivalent loop for the portable ES2022 check; focused evidence tests passed afterward. All18 focused production browser tests passed, including valid nonnumeric-note requests, full source extraction and reload. Independent scoped review found no remaining issues. Previous uncertainty, arithmetic and storage contracts remain covered.

This increment improves source answer selection. It does not establish general language generation, live semantic quality or leading-model parity; those completion requirements remain open.

A concrete preliminary live plan was prepared offline at `/tmp/jev-chat-live-plan-2026-09-18-v3.json`: engine3, ten named development cases and at most20 Jev requests. It covers capabilities, source questions/unknowns, comparison, summary, contextual reference, fiction and source-instruction handling. These are development fixtures, not held-out quality evidence. A configured key is present in `.env.local` (presence only checked; no value printed or provider call made). Explicit authorization for this billable run is the next requested input; automated tests remain offline. Live comparative/independently reviewed quality evidence is still absent.

## Current increment: creative continuity

Keep implementation in the playground's portable engine. A standalone app will consume the same engine when product needs justify a second application; moving UI does not establish better semantics.

- [x] Replace generic story fragments with bounded obstacle-specific causal recipes, deterministic tone/ending/detail controls, and validated story frames. The isolated implementer owns only `story.ts` and its unit tests.
- [x] Carry frames through conversation context, result validation and browser persistence. Re-render frames against the exact displayed text before using them; preserve unaffected story choices during revisions.
- [x] Add typed Jev edit decisions and conservative demo follow-ups within the existing two-request budget. Ambiguous/unsupported edits clarify; malformed provider answers fail. No free-form provider text enters output.
- [x] Verify revision continuity, confidence gates, saved-frame tampering, mocked live requests and production browser reloads. Review renderer specification before integration quality review.

Live evaluation approval remains pending. This increment changes the engine version and therefore requires a refreshed offline plan before any authorized live run. No provider calls are authorized by the passage of time or automatic goal continuations.

Current creative verification: `pnpm test` passed36 JavaScript+349 TypeScript tests; `pnpm typecheck`, explicit ES2022 checking of story/continuity/browser tests and `pnpm build` passed. Focused production chat browser tests passed20/20. Inspected320px light/dark screenshots of restored story controls. Independent renderer spec review passed13 focused tests plus1,944 scripted combinations, with no correctness gaps; integration quality review is still pending. These are offline/mock checks, not live semantic evidence. No commits or provider calls. The preliminary ten-case plan was regenerated offline for engine4 at `/tmp/jev-chat-live-plan-2026-09-18-v4.json`, still capped at20requests and not executed. Python was not rerun for this TypeScript-only increment.

Final creative review found and resolved two boundary bugs with failing-then-passing regressions: fixture ingestion now requires assistant role plus exact story-frame rendering before benchmark planning, and engine frame metadata is validated/detached before JSON sizing so accessors are rejected without execution. Independent re-review confirmed both repros fixed; no remaining scoped findings. Fresh final checks:36 JavaScript+350 TypeScript tests, typecheck, explicit ES2022 test checking, production build, and20/20 focused production browser tests all pass; `git diff --check` passes.

Remaining creative design question: the current confidence gate covers all new-scene fields even where the user permits several choices. A future constrained/delegated/preserved field contract could use seeded rules for confidently delegated choices while retaining the existing gates for explicit constraints and final assessment. This is a proposed experiment, not implemented behavior or measured usability improvement. Live semantic/creative quality and leading-model parity remain unproven; the goal stays active.

## Current increment: explicit constraints and creative freedom

The prior goal turn was **progress**: implemented and independently reviewed story continuity, with350 TypeScript+36 JavaScript tests and20 production browser cases passing. The full quality objective remains unproven. This increment implements the identified selection-contract experiment without changing confidence thresholds or enabling live calls.

- [x] Distinguish constrained, delegated and preserved fields in typed Jev decisions. Gate field-mode interpretation, explicit values, request support and the final response at the existing threshold; keep complete malformed-answer rejection.
- [x] Resolve confidently delegated details through deterministic local rules. Revisions preserve untouched fields and choose a different value for explicitly delegated changes. Response detail stays bound to the interface unless a gated text edit changes it.
- [x] Add mixed-constraint, uncertain-delegation, malformed-answer, preserved-state and replay regressions. Carry the same engine through mocked live browser flows within the two-call budget.
- [x] Independently review the policy and integration, then run focused verification. Refresh the offline plan after the engine version changes; authorized live adherence/clarification measurements remain outstanding.

Engine5 field-policy checks passed363 TypeScript+36 JavaScript tests, typecheck, explicit ES2022 checking, build and22 production browser tests. Independent policy review covered2,184 mixed revisions and integration review found no implementation defects. It did reproduce a second false-clarification condition: all fictional candidates can pass individual acceptance while the final Choice distribution is spread across several acceptable alternatives.

- [x] Extend this increment with a fiction-only final contract: retain per-candidate acceptance at noul>=0.8, then use typed two-level scores to rank accepted candidates with deterministic ties. Factual Choice gates remain unchanged. Malformed/missing scores fail; ranking is a model preference, not independently verified quality.
- [x] Preserve score answers through strict benchmark snapshots and offline replay, update mocked transports and reject unsupported candidates regardless of ranking. Reverify the final engine version and regenerate its offline plan.

Primary API contract verified at https://docs.typesafe.ai/primitives/score and https://docs.typesafe.ai/confidence : score positions span zero through the highest rubric index; two-level rubrics therefore use0–1. Confidence describes distribution concentration and is not independent correctness evidence. No provider calls were made.

Engine6 implementation and verification: typed field modes preserve the existing .8 constraints gates; local seeded selection owns delegated details; fiction acceptance remains noul>=.8 and preference scores only rank accepted candidates. Strict score snapshots and replay reject altered selection outcomes and malformed distributions. Independent ranking spec review passed12 tests plus729 ranking combinations. Fresh full checks passed36 JavaScript+377 TypeScript tests, typecheck, explicit ES2022 test checking, production build, and22 focused production browser tests, including malformed-score UI failure with no fabricated assistant response. Final integration quality review remains pending. The offline ten-case/20-request plan is `/tmp/jev-chat-live-plan-2026-09-18-v6.json`; no live execution or credits consumed. Earlier versioned plans are superseded. Corrected the evaluation guide to match the CLI's existing mechanical-failure exit status.

Final engine6 integration review found no actionable issues. Its74 targeted tests and independent mocked probes confirmed exact acceptance boundaries, zero-confidence preference scores, stable ties, validation of rejected candidates, support-only revision requests, unchanged factual Choice gates, and failed-score benchmark import. Root verified22/22 final production chat browser tests and the latest browser test TypeScript check. `git diff --check` passes. No commits, deployment, provider calls or credits were used. Python coverage was not rerun for this TypeScript-only increment.

The full goal remains active and unproven: live semantic adherence, creative quality, latency/cost and matched leading-model comparison have no current measured evidence. The pending20-request live evaluation authorization remains required before spending the configured key; the current reviewable plan is engine6. Partial constrained fields with several acceptable values remain a known conservative-clarification limitation.

## Current increment: sets of acceptable creative values

The preceding architecture-advice turn made **no implementation progress**. Current inspection confirms the next actionable limitation: a constrained field requires one concentrated Choice even when several values satisfy an exclusion or partial requirement. This increment retains the playground and portable engine boundary.

- [x] Replace the seven creative value Choices with independent per-value acceptance nouls. Keep action, intent, field-mode and whole-response gates unchanged; include only values at or above 0.8. Parse every expected judgment, including unused fields; invalid or incomplete answers fail.
- [x] Resolve constrained fields from explicit accepted sets using deterministic, canonical vocabulary order and fixed seed positions. Reject empty constrained sets. Preserve seeded delegation, revision continuity, and interface style defaults. Validate all sets without invoking accessors; no implicit fallback to excluded values.
- [x] Verify singleton requirements, exclusions, multiple acceptable values, empty/uncertain sets, malformed answers, revisions, persistence and benchmark replay. Validate worst-case question and serialized request bounds with the existing API validator; keep two requests and existing limits.
- [x] Update the production browser scenario, documentation and engine version. Run focused and full verification, policy specification review, then integration quality review. Regenerate the offline ten-case plan without executing it.

Independent field sets do not solve arbitrary correlated constraints: the complete composed response must still pass acceptance. This increment does not claim exhaustive constraint search, live adherence, measured performance or leading-model parity. The outstanding live-run question remains pending; no billable calls are authorized by this continuation.

Engine7 verification: the new integration suite reproduced five failures before implementation and now passes all six scenarios. It also exposed a sampler defect: direct LCG modulo five collapsed several common seeds to the same permitted character. Deterministic uint32 mixing now supplies variety in accepted pools and delegated revisions while preserving the existing delegated-new four-slot sequence. This is not cryptographic randomness. Independent policy review passed14 focused tests,120 five-value pool cases and15 additional malformed-set probes with no getter/coercion executions. Explicit constraints may retain an already acceptable prior value; no-requested-edit rejection remains all-preserved modes with unchanged UI style.

Fresh checks passed36 JavaScript+387 TypeScript tests, project typecheck, explicit ES2022 checking of changed test files, production build and22/22 production chat browser scenarios. The first browser run exposed an outdated failure mock that rejected every newly introduced value, preventing its intended second-stage503; the mock now supplies accepted values and verifies two requests. The expanded browser scenario exercises a singleton constraint, an exclusion-based revision, reload, a delegated setting revision and a malformed final score without fabricated output. Integration quality review is pending. The new offline ten-case/20-request plan is `/tmp/jev-chat-live-plan-2026-09-18-v7.json`; engine6 and earlier plans are superseded. No real provider calls, commits, deployments or credits consumed. Python was not rerun for this TypeScript-only increment.

Final integration quality review passed80 focused tests and independent probes for all seven constrained revision fields, preserved-state clarification, UI detail, malformed unused answers even under noncreative/uncertain/clarify decisions, and factual intent isolation. No actionable findings. The maximum-cardinality fixture uses97 questions and72,461 serialized characters within existing limits; larger text remains subject to the existing size guard. Root confirmed the final22/22 production browser run and explicit test typecheck. The full objective remains active: held-out live semantics, creative quality, latency/cost and matched independently reviewed leading-model comparisons are still unmeasured. The pending live-run authorization has not arrived.

## Current increment: distinct complete creative candidates

The previous goal turn was **progress**: engine7 accepted sets, seeded variety and end-to-end preservation are implemented and verified. Current inspection confirms that `storyPlans` still renders all new-scene candidates from one value combination, and revisions expose only that one combination. A whole-response rejection therefore cannot select an available alternative combination.

- [x] Add a pure bounded candidate resolver alongside the existing single-value policy. Keep the primary resolution identical, then select up to two distinct valid assignments using deterministic diversity across the allowed Cartesian product. Enumerate at most the fixed vocabulary product; retain only the selected assignments. All accepted-set, preserved-field, delegated-revision and style contracts remain enforced.
- [x] Carry resolved alternatives into complete story plans. New scenes retain three wording variants, using distinct assignments when available. Revisions retain their original wording variant, theme and untouched fields while offering up to three permitted assignments. Preserve existing plan IDs for primary plans and bound alternative revision IDs explicitly.
- [x] Assess every complete candidate through the existing second request; preserve the same acceptance and ranking gates, request limits and cancellation. Update saved-result validation for alternate revision IDs without weakening frame/render verification or legacy restoration.
- [x] Reproduce a correlated-constraint case where one combination fails but another satisfies the request; cover singleton/exhausted pools, revisions, malformed assessments, replay, saved continuity and the production UI. Independently review policy specification, then integration quality, and run the required focused verification.

The candidate set is a bounded search proposal, not an exhaustive semantic solver. Diverse assignments increase the alternatives available to the whole-response judge; actual constraint interpretation and final acceptance still require live measurement. No model-authored prose/code, dependency changes or billable calls are introduced.

Engine8 implementation retains the original primary policy and adds streaming Cartesian search for at most two further assignments, maximizing minimum Hamming distance with stable ties. Independent specification review passed22 focused policy tests and an independent48-scenario Cartesian oracle. Four integration regressions failed before the feature and now pass, with an additional singleton-wording compatibility case. The correlated request offers keeper/coast or mechanic/library; seed0's incompatible mechanic/coast primary can now be rejected while a compatible alternative is selected. Saved revisions preserve their wording variant and theme. A further red-to-green persistence regression now rejects same-variant candidate-ID substitutions by binding the selected trace candidate's exact text to the displayed response; missing, duplicate and altered selected candidates also reject.

Fresh checks passed36 JavaScript+400 TypeScript tests, project typecheck, explicit ES2022 test checking, production build and22/22 production browser cases. The browser scenario forces selection of an alternate revision, reloads it and verifies the next turn's previous_story is the selected alternative. Formatting and diff checks pass. Integration quality review is pending. The refreshed offline ten-case/20-request plan is `/tmp/jev-chat-live-plan-2026-09-18-v8.json`; earlier plans are superseded. No provider calls, credits, commits or deployment. Python coverage was not rerun for this TypeScript-only increment.

Final integration review passed97 focused tests and independent probes selecting alternate2 for each original wording variant, exact restored-frame/graph binding, same-variant ID-swap rejection and subsequent revision identity. Synthetic engine7 framed and legacy create_0 unframed compatibility paths passed. A near-budget run used98,492/52,968-character requests within two calls; an oversized120,492-character first request failed before transport. No actionable findings. The full goal remains active and unproven: candidate diversity does not guarantee semantic coverage, and independently reviewed held-out live response quality, latency/cost and leading-model comparisons remain unmeasured. The existing live-evaluation approval is still pending.

## Current increment: exact source excerpts

The previous goal turn was **progress**: distinct creative candidates and saved alternate identity are implemented and verified. This increment addresses factual-answer precision: current plans quote whole paragraphs even when a shorter exact passage answers the question.

- [x] Add deterministic, bounded English sentence-span proposals from source paragraphs, preserving exact UTF-16 offsets and text. Suppress ambiguous abbreviation/decimal boundaries conservatively; single-span whole paragraphs add no alternative. Proposals are optional, not semantic proofs, and all original paragraphs remain available.
- [x] Assess up to32 excerpts independently in the first Jev batch when story questions are absent. Require both parent relevance and excerpt acceptance at0.8. Prune optional excerpt proposals to preserve the existing request budget without truncating original evidence/history; retain the two-call limit and all response gates.
- [x] Offer an exact-excerpt plan alongside full-paragraph candidates. Keep full parents in citations and whole-response assessment. Bind span offsets into source graph leaves and verify each displayed excerpt against its exact parent during restoration. Reject malformed, stripped or altered metadata without weakening old full-paragraph restoration.
- [x] Verify precision, qualifying-context rejection, omitted/uncertain proposals, request bounds, graph/source tampering, benchmark replay and production browser reloads. Review extraction specification, then integration quality, and document actual limits and unmeasured live quality.

Sentence boundaries and verbatim quotation do not establish entailment. Complete-response assessment must check omitted conditions and references against the full paragraphs. No free-form provider text, external execution, additional dependencies or live-credit consumption is introduced.

Engine9 extraction observed nine red failures before implementation and now passes10 focused tests. Independent specification review found no actionable issues after additional exact-slice, cap, malformed-later-record, no-getter and100 maximum-length punctuation stress probes. Five integration regressions cover focused quotations, parent/whole-response gates, strict malformed judgments, altered or stripped spans with recomputed hashes, escaped-text request pruning and benchmark replay. Full verification passed36 JavaScript+415 TypeScript tests, project typecheck, explicit strict ES2022 test checking and production build. Production browser and integration quality reviews are in progress.

The refreshed offline ten-case/20-request plan is `/tmp/jev-chat-live-plan-2026-09-18-v9.json`; earlier plans are superseded. It contains development cases and has not been executed live. The existing live-run authorization is still pending. No provider calls, credits, commits or deployment. Python coverage was not rerun for this TypeScript-only increment.

Final engine9 verification passed24/24 production chat browser scenarios, including exact short answers, full citations, content verification and saved-history reload without additional calls. Inspected the320px citation screenshot; no horizontal overflow. Formatting and diff checks pass. Independent integration review passed83 focused tests and probes for all four factual intents,2/3/5 span limits, relevance ranking then source ordering, exact0.8 gates, malformed unused judgments, saved-parent consistency and graph mutation isolation. Maximum nonstory cardinality is88 questions; a mocked benchmark pruned32 proposals to16 with99,350/77,573-character requests and passed full replay. No actionable findings. Logs: `/tmp/jev-excerpts-tests.log`, `/tmp/jev-excerpts-build.log`, `/tmp/jev-excerpts-e2e.log`.

The full objective remains active and unproven. These checks establish bounded implementation behavior; independently reviewed held-out live semantic and creative quality, latency/cost and matched leading-model comparison remain outstanding. The pending live-run question has not been answered.

## Current increment: linked conversation evaluation

The previous goal turn was **progress**: exact excerpts and their full-source bindings passed implementation, independent review and production browser verification. Inspection now shows that the benchmark runs fixed conversation snapshots; subsequent cases never consume the actual response produced by a prior case. This prevents it from measuring a complete live conversation trajectory.

- [x] Add a strict single-conversation fixture and offline plan with at most10 sequential user turns, predeclared expectations, immutable topic/notes/seed and optional per-turn detail changes. Reuse the existing evaluation schema for inputs and expectations. Future assistant text and input hashes must not be fabricated in the plan.
- [x] Run each turn through the existing bounded benchmark with one global1–20-request budget. Carry exact previous assistant text, options and story frames into subsequent inputs, including the UI's successful-story detail synchronization. Keep every source/message; reject over-budget context before transport. Continue through valid clarification replies, but stop after an execution failure, provider limit, cancellation or checkpoint failure. No retries or hidden answers.
- [x] Checkpoint complete nested turn artifacts and revalidate them offline. Bind every executed input to the immutable fixture and actual prior outputs; replay typed decisions with the existing verifier. Recompute totals, keep unknown usage unknown and reject altered chains or impossible lifecycle states. Integrity does not authenticate provider execution or semantic quality.
- [x] Integrate offline planning, explicit live execution and offline report verification into the existing CLI. Add synthetic development conversation fixtures and regression tests for continuity, thresholds/failure, budgets, cancellation, replay and CLI behavior. Independently review specification then integration; document scope and run relevant verification.

This increment enables future evaluation; mocked trajectories are not live or held-out quality evidence. The existing live-evaluation approval remains pending and no credits are consumed by this work.

The first linked story scenario exposed an actual routing limitation: `The second one, please.` did not resolve an offered option, preventing creative questions from being included. A direct regression failed before the fix. Engine10 now matches complete ordinal references with leading/trailing courtesy words, retains the exact original message, preserves additional requirements for interpretation, and refuses missing or stale options. The production browser follow-up scenario now uses that phrasing. Initial combined conversation/CLI/continuity checks passed27 tests; project and explicit strict ES2022 test/CLI typechecks passed. Independent review and final verification remain pending.

Fresh verification passed36 JavaScript+435 TypeScript tests, project typecheck, explicit strict ES2022 checking of the CLI and changed tests, production build and24/24 production chat browser scenarios. The15 conversation module tests include a seven-turn large-source case: five complete responses consume10 requests, the sixth fails before transport at the existing50k conversation limit, and the seventh remains not-run. Three report-lifecycle regressions were observed red and fixed: execution after an inner checkpoint stop, an invented failed/null artifact with valid input, and a budget-stop label after every turn completed. Four CLI tests cover offline plans, private/no-overwrite files, mode validation, chained replay/tampering and original independent-report compatibility. Formatting and diff checks pass. Independent specification and integration reviews are pending.

Offline engine10 plans were created at `/tmp/jev-chat-live-plan-2026-09-18-v10.json` (ten development cases/20 requests), `/tmp/jev-chat-conversation-story-plan-2026-09-18-v10.json` and `/tmp/jev-chat-conversation-notes-plan-2026-09-18-v10.json` (four turns/eight requests each). Earlier engine plans are superseded. None were executed live; existing approval remains pending. Logs: `/tmp/jev-conversation-tests.log`, `/tmp/jev-conversation-build.log`, `/tmp/jev-conversation-e2e.log`. No provider calls, credits, commits, deployment or dependency changes. Python was not rerun for this TypeScript-only increment.

Independent specification review found two related partial-report lifecycle gaps: a failed outer turn could omit its stop reason while the report was running, and a finished outer turn could contradict its nested stop reason under the same broad exemption. Both were reproduced red and fixed; the exemption now applies only while the individual outer turn is still running. Re-review passed17 focused tests, rejected all3 independent invalid mutations and imported all20 genuine failure checkpoints. Combined benchmark/conversation/CLI/continuity coverage passed55 tests. Final full tests passed36 JavaScript+437 TypeScript tests and the production build passed again (`/tmp/jev-conversation-tests-final.log`, `/tmp/jev-conversation-build-final.log`). The production browser proof remains24/24 on the same engine10/UI behavior. Integration quality review remains pending.

Final integration quality review passed55 focused tests and independent probes that failed each of26 checkpoint positions in a three-turn run. No further calls followed a checkpoint failure; returned reports and last successful snapshots remained importable, with callback error text redacted. Eight cancellation positions also preserved importable state without subsequent dispatch. Additional ordinal probes preserved negation and extra requirements while resolving complete courtesy references. CLI review confirmed that provider import remains behind explicit live/output/key guards and that offline paths, private new outputs, mixed-mode rejection and independent-report compatibility remain intact. No actionable findings.

The full objective remains active and unproven. This increment provides real trajectory evaluation and fixes one demonstrated conversational defect; it does not establish live semantic/creative quality, measured latency/cost or parity with leading models. The existing live-run authorization is still pending; no credits were consumed.

## Completion audit and current blocker

The preceding goal turn was **progress**: linked conversation evaluation, its lifecycle fixes and the polite-reference repair were implemented and independently verified. This audit re-read current source, test/build/browser receipts, the engine10 plans and the offline report; it does not treat implementation completion as achievement of the full quality objective.

| Requirement | Current evidence | Remaining proof |
| --- | --- | --- |
| Jev-only inference with scripted language | `engine.ts` sends closed typed questions to `jev-latest`; the UI injects `runJev`; the server adapter uses the existing TypeSafe endpoint. The portable engine has no separate free-form generation provider. | Actual live interpretation and response quality. |
| Reliable semantic responses | Source paragraphs/excerpts, exact arithmetic, conversation references and whole-response gates have regression coverage. Missing evidence and provider failures remain explicit. | Reviewed live task completion, qualification preservation, clarification rate and semantic failure cases. |
| Cryptographic reconstruction | Canonical SHA-256 response DAG, source-offset bindings, persisted graph/source verification and benchmark replay are implemented and tested. | These prove internal consistency only; they cannot supply truth or semantic-quality evidence. |
| Creativity and conversational continuity | Bounded accepted-value combinations, scripted story realization, preserved revisions and linked actual-response evaluation are tested. | Independently reviewed creative quality and live multi-turn fidelity; finite scripted vocabulary is still a capability limit. |
| Professional chat UI and guidance |24 production chat browser cases cover responsive screens/themes, keyboard behavior, source details, options, traces, drafts, restore, failure and cancellation. Screenshots were inspected during UI/excerpt work. | Subjective world-class experience is not established by browser assertions. |
| Robustness and performance |473 unit tests pass; request/question/context limits, cancellation, failure checkpoints and no-overwrite exports are covered. Final build and typechecks pass. | Live latency, reported usage/cost and observed semantic reliability. Limits do not establish speed or quality. |
| Playground/standalone architecture | Shared TypeScript engine is used by the playground and CLI through injected adapters. No duplicate standalone implementation or package was introduced. | A separate application is deferred until product needs require another consumer; moving the UI would not establish quality. |
| Evaluation and comparison | Independent-case and linked-conversation plans, bounded execution, replay, comparison import and separate human/model review records are implemented. Current artifacts are plans or offline-demo reports. | Authorized live outputs, separately reviewed held-out cases, matched current leading-model outputs and independent quality judgments. |

Fresh receipt inspection confirms36 JavaScript+437 TypeScript tests with zero failures in `/tmp/jev-conversation-tests-final.log` and24/24 production browser cases in `/tmp/jev-conversation-e2e.log`. Existing Chromium was used successfully; installation was not rerun. Python's earlier26-test proof is historical and was not repeated for the subsequent TypeScript-only changes. The current engine10 plan files declare planning mode; the available28-case offline report declares semantic/creative quality unmeasured and zero human-reviewed cases. No actual live evaluation or comparison result was produced by this work. All implementation/review agents are terminal; there is no live process to wait on.

The pending permission request is for the separate ten-development-case live Jev evaluation capped at20 requests. Its current reviewable artifact is `/tmp/jev-chat-live-plan-2026-09-18-v10.json`; the new conversation plans are additional offline artifacts, not additional authorized spending. `AGENTS.md:51` says, “Never call guessed provider endpoints or consume shared credits during automated tests,” and `AGENTS.md:64` specifies mocked browser responses. The separate billable benchmark was therefore presented for explicit approval; the continuing goal has not answered that pending request.

The same missing live-run authorization and quality evidence persisted through the creative-candidate, exact-excerpt and linked-conversation goal turns and was revalidated in this audit. Independent implementation and evaluation preparation are complete for the proposed trial. The next evidence-driven step needs actual approved Jev results; further changes justified solely by the same development fixtures cannot establish the requested comparative quality. Mark the goal **blocked**, not complete, until approval or externally supplied evaluation artifacts allow that step. Approval of the proposed trial would enable measurement and subsequent fixes, not itself establish leading-model parity. No commit, push, deployment or provider call was performed in this audit.

## Screenshot regression: built-in help and repeated clarification

The user reported that **Explain how Jev works** received `clarify_uncertain` in live mode, then **why are you repeating yourself?** received the same text. The intent probability in the screenshot was below the existing 0.8 gate. The previous engine routed even its own documented help prompts through that probabilistic selection and lacked explicit repetition recovery.

Engine11 recognizes complete documented help requests and bounded repetition-feedback phrases before inference. They render authored help, retain graph verification and saved-history validation, and display **Scripted help** with the selected conversation mode. Their traces record zero requests and tokens, no probability/confidence, and `scripted-help`. Extra instructions, negated or quoted commands, and arbitrary source questions still use normal interpretation and unchanged support gates. Recovery can answer the preceding recognized help request after an old generic fallback; a repeated uncertain selection offers concrete help topics. Provider failures still fail.

The benchmark accepts zero-request completed help only with a matching scripted-help trace and exact offline replay. Request-measurement summaries retain null totals for an empty request set; the individual response trace records known zero usage. Documentation explains the distinction. Existing engine10 benchmark plans must be regenerated before a future run under the versioned replay contract.

Verification:36 JavaScript and442 TypeScript unit tests passed (`/tmp/jev-help-tests.log`); project and explicit strict ES2022 test typechecks passed; production build passed (`/tmp/jev-help-build.log`); all26 production chat browser cases passed on port3002 (`/tmp/jev-help-e2e.log`). Desktop/mobile screenshots of the help label and content were inspected. Coverage includes low-confidence routing, repetition feedback, working follow-up buttons, restore, integrity verification, unknown confidence, and zero dispatch. The authored factual-plan regression now uses a freeform prompt and still requires the full two-request assessment. Independent review found no blocking issue; the confidence guide copy was corrected. Python tests were not repeated for this TypeScript-only change. No real provider calls, commits, pushes, or deployments occurred. This fixes the reported routing/recovery defect without changing the broader goal's blocked status or establishing live semantic quality.
